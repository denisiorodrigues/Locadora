meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: https://localhost:44330/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "denisio@email.com",
    "password": "Senha@123"
  }
}

script:post-response {
  bru.setVar("tokenType", res.body.tokenType)
  bru.setVar("token",  res.body.accessToken); 
}

settings {
  encodeUrl: true
  timeout: 0
}
